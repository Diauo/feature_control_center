# OZON 库存同步功能包源代码

本目录来自甲方提供的 `ozon_Inventory.zip`。适配工作只涉及平台接入：重写元数据、声明 `dataSource.xlsx`、声明 Python 依赖，并优先通过 `ctx.get_data_source_path()` 获取本次执行的数据源快照。库存计算、接口调用和业务返回逻辑保持原样。

发布前必须由业务方在测试店铺完成接口联调。原业务代码中存在关闭 TLS 证书校验的请求，详见交付文档 `04-Ozon适配与上线前检查.md`；平台没有擅自修改该业务行为。
